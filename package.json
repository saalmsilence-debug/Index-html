{
  "name": "bookpesa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve public -l 3000",
    "build": "echo 'Static site — nothing to build by default'",
    "lint": "echo 'Add eslint if you like'"
  },
  "dependencies": {
    "dexie": "^3.2.2",
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {}
}
